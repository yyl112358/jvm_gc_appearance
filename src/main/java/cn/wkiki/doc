在编写此部分代码时的一些思考

1、Byte[] 与byte[] 差别很大 Byte[]内存放的是一个个Byte类型的对象，同时声明和分配内存时对内存空间的连续性并无要求。
因为毕竟他可以简单的视为一个 给定length长度的对象引用容器，引用的是一个个Byte类型的对象。但是对内存的额外消耗很大。同样的
一些代码配置，使用Byte[]可能最终执行的预期与期望不符，甚至如果内存配置较为严苛的话，会发生OOM。但是byte[]就和我们理解的一整块
连续的内存区域一直，在验证内存分配时更加符合期望。

2、在使用jdk 版本为1.8时，指定不同的收集器输出的日志格式千差万别。